import 'package:flutter/material.dart';

// Kelas AppColors menyimpan definisi palet warna yang digunakan di seluruh aplikasi.
class AppColors {
  // Warna latar belakang utama (hitam pekat)
  static const Color background = Color(0xFF000000);
  
  // Warna permukaan untuk kartu, dialog, dan kontainer (abu-abu sangat gelap)
  static const Color surface = Color(0xFF1E1E1E);
  
  // Warna utama/aksen aplikasi (ungu violet)
  static const Color primary = Color(0xFF8A2BE2);
  
  // Warna untuk indikasi jawaban benar atau sukses (hijau emerald)
  static const Color success = Color(0xFF2ECC71);
  
  // Warna untuk indikasi jawaban salah atau kegagalan (merah alizarin)
  static const Color failure = Color(0xFFE74C3C);
  
  // Warna teks utama (putih)
  static const Color textPrimary = Colors.white;
  
  // Warna teks sekunder atau petunjuk (abu-abu)
  static const Color textSecondary = Colors.grey;
}
